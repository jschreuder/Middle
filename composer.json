{
    "name": "jschreuder/middle",
    "description": "Middleware based micro-framework build on components by everyone else",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Jelmer Schreuder",
            "email": "j.schreuder@mijnpraktijk.com"
        }
    ],
    "minimum-stability": "stable",
    "require": {
        "psr/http-message": "^1.0",
        "psr/log": "^1.0"
    },
    "require-dev": {
        "php": ">=7.0",
        "phpspec/phpspec": "^3.0",
        "henrikbjorn/phpspec-code-coverage": "^3.0",
        "twig/twig": "^1.24",
        "zendframework/zend-session": "^2.7",
        "lcobucci/jwt": "^3.1",
        "dflydev/fig-cookies": "^1.0",
        "symfony/routing": "^3.1"
    },
    "suggests": {
        "twig/twig": "Required when using the TwigRenderer view-engine implementation",
        "symfony/routing": "Required when using the SymfonyRouter implementation",
        "zendframework/zend-session": "Required when using the ZendSession implementation",
        "lcobucci/jwt": "Required when using the JwtSession implementation",
        "dflydev/fig-cookies": "Required when using the JwtSession implementation"
    },
    "config": {
        "bin-dir": "bin"
    },
    "autoload": {
        "psr-4": {
            "jschreuder\\Middle\\": "src"
        }
    }
}
